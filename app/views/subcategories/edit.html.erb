<h1>Update article<span><%= link_to 'back', { action: 'new', id: @subcategory.category_id }, class: 'btn btn-default btn-xs padding-left' %></span>
</h1>

<%= bootstrap_form_for @subcategory,  url: { action: :update, id: @subcategory.id} do |f| %>
  <% if @subcategory.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited
      this article from being saved:</h2>
    <ul>
    <% @subcategory.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.text_field :name, label: 'Name of the article', placeholder: 'Write a new article...' %>
    <%= f.text_area :body, label: 'Article', rows: 10, placeholder: 'Write text for the article...' %>
  </p>
  <p>
    <%= f.submit 'Update Article' %>
  </p>
<% end %>


